# UtilityCraft Addon Template v1.0.1

## FIXED
- Restricted multiblock entity lookup and deactivation to live dorios:multiblock controllers, preventing hide events and removal from targeting players, dropped items or visual entities.

## CHANGED
- Removed explicit UtilityCraft BP/RP dependencies from the template manifests. The template keeps its own BP/RP linkage and Minecraft API dependencies; examples still use UtilityCraft resources when enabled in the same world.
